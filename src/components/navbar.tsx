import styles from '../styles/navbar.module.scss';

const Navbar = () => {
    return (
        <div className={styles.navbar}>
            <div>navbar</div>
        </div>
    );
};

export default Navbar;